# 串連系統 v2.2 — 人工參數方法論

> 「**主管排序決定方向，等價關係決定初始尺度，測資結果決定調整幅度，敏感度分析確認穩定性。**」

本系統的人工權重並非由開發者任意指定，而是經過五階段流程設計：

```
整合管理層意見 → 等價關係 → 弱先驗 → 測資校準 → 敏感度確認
```

每一階段都能對應到具體的設計動作與可審查的證據。

---

## 為什麼需要這份文件？

本系統有 **3 組人工權重**：

| 模組 | 權重參數 | 數量 |
|---|---|:---:|
| 員工負載分析 | 案件 / 卡點 / 交接 / 被提及 | 4 個主權重 + 衰減序列 |
| 組織健康度雷達 | 6 維度權重 | 6 個 |
| BM25F 智能搜尋 | 6 欄位權重 | 6 個 |

合計約 16 個人工參數。每個都能用這套五階段流程說明：「為什麼是這個數字？」

---

## 五階段方法論

### ① 整合管理層意見 — 決定方向

**問題**：哪個訊號比較重要？

**作法**：與合作公司「串聯」的 **5 位管理層**進行簡易結構化問卷訪談，整合他們對「員工負載、組織卡點、決策影響」三類訊號的重要性排序，並對照業界文獻（Andy Grove、Edmondson、McKinsey OHI）交叉驗證。

**員工負載排序：**
```
卡點 > 案件 > 交接 > 被提及
（影響半徑 > 直接負擔 > 中介工作 > 涉入度）
```

**BM25F 欄位排序：**
```
標題 > 摘要 > Tag > 負責人 > 內文 > 留言
（資訊密度由高到低）
```

**健康度 6 維排序：**
```
流動性 > 卡點 = 成員健康 > 溝通 = 決策 > 負載均衡
（對組織存續的影響度）
```

**文獻錨點：**
- Andy Grove《High Output Management》— 管理時間單位 2 週
- Edmondson《The Fearless Organization》— 溝通+心理安全為核心
- McKinsey OHI 9 維度模型 — 健康度維度選擇依據

---

### ② 等價關係 — 決定初始尺度

**問題**：「一件 A 大概等於幾件 B？」

**作法**：把抽象重要性翻成具體比例。

**員工負載：**
> 「**一件卡點大概等於 2~3 件普通案件**」 → 取 2.5

| 等價關係 | 解讀 |
|---|---|
| 1 個卡點 ≈ 2.5 個案件 | 卡點不只影響自己，還擋住別人 |
| 1 個案件 ≈ 2 個被提及 | 自己做事比被 cc 重很多 |
| 1 個案件 ≈ 1.25 個交接 | 交接有部分責任不在自己 |

**BM25F 欄位：**
> 「**標題 1 次命中 ≈ 內文 5 次命中**」 → 比例 5:1

| 等價關係 | 解讀 |
|---|---|
| 標題 1 次 ≈ 內文 5 次 | 標題濃縮整個案件意圖 |
| 摘要 1 次 ≈ 內文 4 次 | 摘要是精煉版內文 |
| Tag 1 次 ≈ 內文 2 次 | 結構化但訊息稀 |

---

### ③ 弱先驗 — 不要一開始就放大膽

**問題**：根據排序先給小幅差距，避免過度自信

**作法**：依排序給小差距，**讓資料來把它撐開或合攏**

**員工負載弱先驗版本（v0）：**
```
卡點 1.3、案件 1.2、交接 1.1、被提及 1.0
```

差距小、方向對、不過度承諾。

**理論依據**：這就是 Bayesian 統計的 **weak prior**（弱先驗）概念 — 在沒有強證據前不要把分佈設太尖。

**vs 強先驗（容易出問題）：**
```
卡點 5.0、案件 3.0、交接 1.5、被提及 1.0   ← 一開始就放大差距
```

如果排序錯了，強先驗的錯誤代價更大。

---

### ④ 測資校準 — 決定調整幅度

**問題**：用 SEED 跑，看實際分數分佈合不合理

**作法**：用 SEED data 跑出全員 loadScore 分佈，觀察分位數位置，
依此決定「過載 / 高負擔 / 一般 / 低負載」四級的門檻。

**SEED 真實分佈（程式碼 `algorithms.ts:99-211` 跑出）：**

| 排名 | 員工 | loadScore | percentile |
|:---:|---|:---:|:---:|
| 1 | 林聿平 | **10.50** | 100% |
| 2 | 周世倫 | 7.50 | 95% |
| 3 | 梁嘉芫 | 7.32 | 89% |
| 4 | 鍾皓明 | 6.55 | 84% |
| 5 | 蔡明遠 | 4.35 | 79% |
| ... | （其他 15 人） | < 4 | < 75% |

**等級門檻（程式碼真實值）：**
```
loadScore ≥ 25 或 percentile ≥ 90  →  overload 過載
loadScore ≥ 15 或 percentile ≥ 75  →  high 高負擔
loadScore ≥ 6                       →  normal 一般
loadScore ≥ 1                       →  low 低負載
< 1                                 →  idle 閒置
```

**為什麼是「絕對分數 OR 相對 percentile」雙重判定？**

- 絕對分數 25：抓出「**真的爆掉**」的員工（多到任何環境都該被警示）
- 相對 percentile 90：抓出「**在這個組織裡相對最忙**」的員工（即使絕對分數不到 25）

→ 在 SEED 中林聿平絕對分 10.5 不到 25，但 percentile 100% 觸發過載警示。
→ 這個雙閘設計能同時處理「**全公司一起忙**」和「**單人爆掉**」兩種情境。

**這層不是「自由地調權重」 — 是「**用真實分佈反推門檻**」**。權重已由 ① ② ③ 三層決定，校準層只負責劃出各級界線。

---

### ⑤ 敏感度確認 — 確認穩定性

**問題**：萬一權重錯一點點，結論會崩嗎？

**作法**：把權重 ±10%~20% 隨機擾動，跑 500 次，看 Top-N 排名變不變

**真實實驗結果**（員工負載權重）：

| 擾動幅度 | Top-3 名單穩定 | Top-5 名單穩定 | Spearman ρ |
|:---:|:---:|:---:|:---:|
| ±10% | **99.4%** | 99.2% | 0.998 |
| **±20%** | **93.4%** | **95.8%** | **0.995** |
| ±30% | 89.8% | 93.5% | 0.989 |
| ±50% | 83.6% | 89.7% | 0.977 |

**程式碼**：`scripts/sensitivity_analysis.ts` ── 任何人可重跑

```bash
npx tsx scripts/sensitivity_analysis.ts
```

**結論**：
- Top-3 在 ±20% 擾動下 93.4% 穩定 → **系統不依賴精確權重**
- Spearman ρ = 0.995 → 排名相關性接近完美
- 證明模型靠的是「**排序原則**」而非「**精確數字硬湊**」

---

## 對應方法論的 Q&A 速查

| 教授問 | 用哪一階段答 |
|---|:---:|
| 「為什麼卡點權重比交接大？」 | ① 排序 — 整合管理層意見 + 業界文獻 |
| 「為什麼是 1.5 不是 1.3？」 | ② 等價關係 — 「2 個交接 ≈ 1 個卡點」 |
| 「為什麼一開始不直接設 5：1？」 | ③ 弱先驗 — 避免過度承諾 |
| 「過載門檻 25 怎麼來的？」 | ④ 校準 — 從 SEED 真實分佈推出 |
| 「萬一數字錯了系統會崩嗎？」 | ⑤ 敏感度 — ±20% 內 93.4% 穩定 |
| 「整套有什麼缺點？」 | 見下節 |

---

## 方法論的弱點（誠實揭露）

| 階段 | 弱點 | 補救 |
|---|---|---|
| ① 排序 | 訪談樣本數有限，可能不代表整個產業 | 文獻交叉參照、未來可擴大訪談規模 |
| ② 等價 | 等價關係本身仍主觀 | 寫成文件、可逐條挑戰 |
| ③ 弱先驗 | 「弱」的程度沒有客觀標準 | 用 Bayesian 標準作法（差距 ≤ 30%） |
| ④ 校準 | SEED 是自編的，有 self-validation 風險 | 用極端錨點 + 多測資 |
| ⑤ 敏感度 | 證明 robustness 不證明 correctness | 老實承認 — 需 ground truth 補上 |

**整體結構性弱點：** 缺乏真實 ground truth → 所有層的「合理性」都建立在團隊內部直覺。

---

## 最佳作法（Future Work）

```
現狀（我們）                              最佳作法
─────                                  ─────
① 整合管理層意見           ──→            (已涵蓋)
② 團隊內部等價             ──→            3-5 位主管獨立標註 + Cohen's κ
③ 弱先驗 (1.3:1.2:1.1)    ──→            同（這層不依賴真實資料）
④ 自編 SEED               ──→            真實 6-12 月歷史 + Grid/Bayesian Search
⑤ ±擾動 500 次            ──→            + Cross-validation + MAP@K / NDCG@K
                          ──→            + A/B test + Online Learning
```

**主要差距**：ground truth 資料集（用於 ②-⑤ 的量化評估）。是錢買來的，不是腦力換得到的。

**設計取捨**：本系統選擇「**可解釋人工權重**」而非「**機器學習自動學權重**」 — 因為使用者（主管）需要對員工說明評分依據，黑箱模型在管理場景不適用。

---

## 程式碼與資料對應

| 階段 | 程式碼 / 文件位置 |
|---|---|
| ① 排序 | `src/lib/algorithms.ts:99-178`（權重定義 + 註解） |
| ② 等價 | 本文件第 ② 節 |
| ③ 弱先驗 | 本文件第 ③ 節（設計理由） |
| ④ 校準 | `src/lib/seedData.ts`（測資）+ 本文件第 ④ 節 |
| ⑤ 敏感度 | `scripts/sensitivity_analysis.ts`（可重跑） |

---

## 一句話總結

> **這套方法論的價值不在「證明 1.5 是最佳值」，而在「**讓 1.5 變成可審查的設計決策**」。**

---

**版本**：v2.2.2　**最後更新**：2026-05　**作者**：第 13 組
